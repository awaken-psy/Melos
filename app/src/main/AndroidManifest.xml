<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true">

        <!-- Main launcher: 3-tab UI (simulate / collector / log) -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Melos"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Collector map (reused from collector module) -->
        <activity
            android:name=".collector.MapActivity"
            android:theme="@style/Theme.Melos" />

        <!-- Trajectory map preview -->
        <activity
            android:name=".TrajectoryMapActivity"
            android:theme="@style/Theme.Melos" />

        <!-- Location picker for fixed-point check-in -->
        <activity
            android:name=".LocationPickerActivity"
            android:theme="@style/Theme.Melos" />

        <!-- Collector foreground service -->
        <service
            android:name=".collector.RecordingService"
            android:foregroundServiceType="location" />

        <!-- Xposed / LSPosed module metadata -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="@string/xposed_description" />
        <meta-data
            android:name="xposedminversion"
            android:value="82" />
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />
        <provider
        android:name=".ConfigProvider"
        android:authorities="com.melos.config"
        android:exported="true" />
</application>

</manifest>
